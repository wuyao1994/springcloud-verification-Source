spring:
  application:
    name: user-service

---
spring:
  cloud:
    config:
    discovery:
      label: feature/ACCP-8-spring-streaming-data-integration-with-kafka
      enabled: true
      service-id: config-service
      profile: docker
