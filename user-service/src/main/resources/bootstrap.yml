spring:
  application:
    name: user-service

---
spring:
  cloud:
    config:
#      discovery:
#        enabled: true
#        service-id: config-service
      uri: http://192.168.0.114:8080
      profile: docker
      label: dev
