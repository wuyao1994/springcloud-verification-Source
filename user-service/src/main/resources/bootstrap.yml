spring:
  application:
    name: user-service

---
spring:
  cloud:
    config:
#      discovery:
#        enabled: true
#        service-id: config-service
      uri: http://192.168.0.114:8080
      label: dev
      profile: docker
management:
  security:
    enabled: false